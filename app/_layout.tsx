// Minimal placeholder layout so TypeScript stops reporting errors on this path.
// The app actually starts from App.tsx with React Navigation.

import { View } from 'react-native';

export default function Layout() {
  return <View />;
}
