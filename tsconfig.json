{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    // Use moduleResolution that works with Expo's customConditions
    "moduleResolution": "bundler",
    "strict": false,
    "baseUrl": ".",
    "paths": {
      "@/*": [
        "./*"
      ]
    }
  }
}
